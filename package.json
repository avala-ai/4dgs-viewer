{
  "name": "@4dgs/viewer",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "bootstrap:sdk": "node scripts/bootstrap-sdk.mjs",
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "npm run build && npm run test:decode && npm run test:browser",
    "test:decode": "node --test --test-concurrency=1 --test-timeout=120000 test/decode.test.mjs",
    "test:browser": "node --test --test-concurrency=1 --test-timeout=120000 test/browser.test.mjs"
  },
  "dependencies": {
    "@4dgs/browser": "file:.sdk-packs/4dgs-browser.tgz",
    "@4dgs/core": "file:.sdk-packs/4dgs-core.tgz",
    "react": "^19.2.0",
    "react-dom": "^19.2.0"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^5.0.0",
    "vite": "^7.0.0"
  },
  "engines": {
    "node": ">=22"
  }
}
